<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App_CrediVnzl.Pages.SolicitudPendientePage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Spacing="30" Padding="30" VerticalOptions="Center">

            <!-- Icono principal mejorado -->
            <Frame BackgroundColor="#FF9800"
                   CornerRadius="60"
                   WidthRequest="120"
                   HeightRequest="120"
                   Padding="0"
                   HasShadow="True"
                   HorizontalOptions="Center">
                <Label Text="{StaticResource IconClock}" 
                       FontSize="60" 
                       TextColor="White"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"/>
            </Frame>

            <VerticalStackLayout Spacing="10">
                <Label Text="Solicitud Enviada" 
                       FontSize="32" 
                       FontAttributes="Bold" 
                       HorizontalOptions="Center"
                       TextColor="#FF9800"/>
            </VerticalStackLayout>

            <Frame CornerRadius="20" Padding="25" BackgroundColor="White" HasShadow="True">
                <VerticalStackLayout Spacing="20">
                    
                    <Label Text="{Binding NombreNegocio, StringFormat='Tu solicitud ha sido enviada a {0}'}"
                           FontSize="18"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"
                           TextColor="#212121"
                           LineBreakMode="WordWrap"/>

                    <BoxView HeightRequest="1" Color="#E0E0E0"/>

                    <VerticalStackLayout Spacing="15">
                        <!-- Revisión en proceso -->
                        <HorizontalStackLayout Spacing="15">
                            <Frame BackgroundColor="#2196F3" 
                                   CornerRadius="20" 
                                   WidthRequest="40" 
                                   HeightRequest="40" 
                                   Padding="0" 
                                   HasShadow="False">
                                <Label Text="{StaticResource IconSearch}" 
                                       FontSize="18" 
                                       TextColor="White"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"/>
                            </Frame>
                            <VerticalStackLayout Spacing="5" VerticalOptions="Center">
                                <Label Text="Revisión en proceso"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="#212121"/>
                                <Label Text="El administrador revisará tu información"
                                       FontSize="13"
                                       TextColor="#757575"
                                       LineBreakMode="WordWrap"/>
                            </VerticalStackLayout>
                        </HorizontalStackLayout>

                        <!-- Notificación -->
                        <HorizontalStackLayout Spacing="15">
                            <Frame BackgroundColor="#4CAF50" 
                                   CornerRadius="20" 
                                   WidthRequest="40" 
                                   HeightRequest="40" 
                                   Padding="0" 
                                   HasShadow="False">
                                <Label Text="{StaticResource IconMessage}" 
                                       FontSize="18" 
                                       TextColor="White"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"/>
                            </Frame>
                            <VerticalStackLayout Spacing="5" VerticalOptions="Center">
                                <Label Text="Recibirás notificación"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="#212121"/>
                                <Label Text="Te avisaremos cuando sea aprobada"
                                       FontSize="13"
                                       TextColor="#757575"
                                       LineBreakMode="WordWrap"/>
                            </VerticalStackLayout>
                        </HorizontalStackLayout>

                        <!-- Tiempo estimado -->
                        <HorizontalStackLayout Spacing="15">
                            <Frame BackgroundColor="#FF9800" 
                                   CornerRadius="20" 
                                   WidthRequest="40" 
                                   HeightRequest="40" 
                                   Padding="0" 
                                   HasShadow="False">
                                <Label Text="{StaticResource IconTimer}" 
                                       FontSize="18" 
                                       TextColor="White"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"/>
                            </Frame>
                            <VerticalStackLayout Spacing="5" VerticalOptions="Center">
                                <Label Text="Tiempo estimado"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="#212121"/>
                                <Label Text="Normalmente 1-2 días hábiles"
                                       FontSize="13"
                                       TextColor="#757575"
                                       LineBreakMode="WordWrap"/>
                            </VerticalStackLayout>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </Frame>

            <!-- Información adicional -->
            <Frame CornerRadius="15" Padding="20" BackgroundColor="#E3F2FD" HasShadow="False" BorderColor="#2196F3">
                <VerticalStackLayout Spacing="10">
                    <HorizontalStackLayout Spacing="10">
                        <Label Text="{StaticResource IconInfo}"
                               FontSize="18"
                               TextColor="#1976D2"
                               VerticalOptions="Start"/>
                        <Label Text="Mientras tanto..."
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#1976D2"
                               VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                    
                    <VerticalStackLayout Spacing="8" Margin="28,0,0,0">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="{StaticResource IconCheck}"
                                   FontSize="14"
                                   TextColor="#4CAF50"/>
                            <Label Text="Guarda tus datos de acceso en un lugar seguro"
                                   FontSize="13"
                                   TextColor="#424242"
                                   VerticalOptions="Center"/>
                        </HorizontalStackLayout>
                        
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="{StaticResource IconCheck}"
                                   FontSize="14"
                                   TextColor="#4CAF50"/>
                            <Label Text="Mantén tu teléfono activo para recibir notificaciones"
                                   FontSize="13"
                                   TextColor="#424242"
                                   VerticalOptions="Center"/>
                        </HorizontalStackLayout>
                        
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="{StaticResource IconCheck}"
                                   FontSize="14"
                                   TextColor="#4CAF50"/>
                            <Label Text="Puedes intentar iniciar sesión más tarde"
                                   FontSize="13"
                                   TextColor="#424242"
                                   VerticalOptions="Center"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Frame>

            <!-- Botones de acción -->
            <VerticalStackLayout Spacing="15">
                <Button BackgroundColor="#2196F3"
                        TextColor="White"
                        CornerRadius="15"
                        HeightRequest="55"
                        FontSize="16"
                        FontAttributes="Bold"
                        Command="{Binding VolverLoginCommand}">
                    <Button.Text>
                        <MultiBinding StringFormat="{}{0} Intentar Iniciar Sesión">
                            <Binding Source="{StaticResource IconUser}" />
                        </MultiBinding>
                    </Button.Text>
                </Button>

                <Button BackgroundColor="Transparent"
                        TextColor="#2196F3"
                        BorderColor="#2196F3"
                        BorderWidth="2"
                        CornerRadius="15"
                        HeightRequest="55"
                        FontSize="16"
                        FontAttributes="Bold"
                        Command="{Binding VolverInicioCommand}">
                    <Button.Text>
                        <MultiBinding StringFormat="{}{0} Volver al Inicio">
                            <Binding Source="{StaticResource IconHome}" />
                        </MultiBinding>
                    </Button.Text>
                </Button>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
